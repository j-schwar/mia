fun add(x: i32, y: i32) -> i32 = x + y;

fun sub(x: i32, y: i32) = x - y;

fun mul(x, y) -> i32 = x * y;

fun div(x, y) = x / y;

fun is_pythagorean_relation(a: u32, b: u32, c: u32) -> bool {
  (c * c) == ((a * a) + (b * b));
}
